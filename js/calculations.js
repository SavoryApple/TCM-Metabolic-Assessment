// Main calculations file
// This file coordinates all the modules

// Note: All functions are defined in separate files:
// - utils.js: Helper functions (getSectionNumber, getSectionDisplayName, determineCategory, printDocument)
// - scoring.js: Score calculations (calculateSectionTotal, calculateSubsectionScore, updateFiveElementInteractions)
// - recommendations.js: Recommendation logic (generatePractitionerRecommendations, analyzeEndocrineSquare, analyzeClinicalPatterns, calculateTestCosts)
// - display.js: UI rendering (displayPractitionerRecommendations, generateSymptomSections)
// - ui-handlers.js: Event handlers (handleButtonClick, DOMContentLoaded initialization)

// All modules are loaded via script tags in index.html in the correct order
console.log("Calculations module loaded - all functions available from separate modules");